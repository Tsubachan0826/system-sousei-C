#include <stdio.h>
int main(void)
{
	int count;

	for (count = 1; count < 10000; count++)
	{
		if (count % 221 == 0)
		{
			printf("%d FizzBuzz\n", count);
		}
		else if (count % 13 == 0)
		{
			printf("%d Fizz\n", count);
		}
		else if (count % 17 == 0)
		{
			printf("%d Buzz\n",count);
		}
	}
	return 0;
}
